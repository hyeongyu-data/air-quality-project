[pytest]
# 저장소 루트를 먼저 둔다. consumer/ producer/ 만 두면 `producer`가 패키지가
# 아니라 producer/producer.py 모듈로 잡혀 패키지 import 테스트가 불가능하다.
# 뒤의 두 경로는 rules·contract 같은 순수 모듈을 최상위 이름으로 쓰기 위한 것이다.
pythonpath = . consumer producer
testpaths = tests
